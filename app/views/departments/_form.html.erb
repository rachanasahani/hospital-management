<%= form_with model: [@hospital, @department] do |form| %>
  <% if department.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

      <ul>
        <% department.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :department_code, style: "display: block" %>
    <%= form.text_field :department_code %>
  </div>

  <div>
    <%= form.label :department_name, style: "display: block" %>
    <%= form.text_field :department_name %>
  </div>

  <div>
    <%= form.label :hospital_id, style: "display: block" %>
    <%= form.text_field :hospital_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
